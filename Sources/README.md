#  Overview
This package is all about making communication between Client/Server **Type Safe** and easy.

The main idea is to create a single Struct per each route in your server. This structs are created in a Shared package that both server/client have access to, On those type you have enter the base requirements for your routes, including path, http method, ... .


